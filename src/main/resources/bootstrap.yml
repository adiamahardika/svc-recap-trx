spring:
  cloud:
    config:
      uri: http://localhost:8888
      label: v1
      profile: preprod
  application:
    name: svc-scheduler-transaction-log
management:
  endpoints:
    web:
      exposure:
        include: refresh, health, info
